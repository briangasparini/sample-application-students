git :
  depth : 5
stages :
  - "Build and Test"
  - "Package"
jobs :
  include :
    - stage : "Build and Test"
      language : java
      jdk : oraclejdk11
      before_script :
        - cd sample-application-backend
      script :
        - echo "Maven build"
        - mvn clean verify
        - echo "Run test coverage and Quality Gate"
    - stage : "Build and Test"
      language : node.js
      node_js : "12.20"
      before_script :
        - cd sample-application-frontend
      script :
        - echo "NPM install and build"
        - npm run test
    - stage : "Package"
      before_script :
        - cd sample-application-backend
      script :
        - echo "Docker build ..."
        - docker build -t sample-application-students_backend .
        - echo "$DockerHubPwd" | docker login -u "$DockerHubID" --password-stdin
        - echo "Docker push ..."
        - docker push briangasparini/dockerhub01:Dev
      on:
        branch: developBranch
    - stage : "Package"
      before_script :
        - cd sample-application-frontend
      script :
        - echo "Docker build ..."
        - echo "Docker login ..."
        - echo "Docker push ..."
cache :
directories :
  - "$HOME/.m2/repository"
  - "$HOME/.npm"
services :
- docker